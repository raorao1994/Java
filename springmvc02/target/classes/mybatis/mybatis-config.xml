<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 全局setting配置，根据需要添加 -->
	<!-- 配置别名 -->
	<typeAliases>
		<package name="com.raorao.ssm.po" />
	</typeAliases>
	
	<!-- 配置mapper
		由于springmvc和mybatis的整合包进行mapper扫描，这里就不需要在进行配置了
		但是必须遵循：mapper.xml和mapper.java文件名相同，并且在同一个文件夹下
	 -->
	 <!-- <mappers><mappers> -->
</configuration>